# Python dependencies for the lidar2mesh pipeline.
#   pip install -r requirements.txt
#
# Version markers are lower bounds (known-good releases). Run `pip freeze`
# to capture an exact lockfile for reproduction.

argh
laspy>=2.7.0
mesh-to-sdf
numpy>=2.2.6
open3d>=0.19.0
pandas>=2.3.3
pillow>=12.1.1
plotly>=6.6.0
plyfile
pygame
pyyaml
rerun-sdk>=0.30.2
scipy
trimesh
vdbfusion

# GPU / external dependencies — NOT installable from PyPI in a standard way.
# On the team cluster these are supplied by the shared `nksr` conda env via a
# site-packages .pth (see README "Dependencies"). Install separately otherwise:
#   torch       — CUDA build matching your driver (https://pytorch.org)
#   nksr        — Neural Kernel Surface Reconstruction (https://github.com/nv-tlabs/NKSR)
#   pcdmeshing  — block Delaunay mesher (https://github.com/cvg/pcdmeshing)
